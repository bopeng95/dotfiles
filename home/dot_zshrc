export EDITOR="nvim"
export EZA_CONFIG_DIR="$HOME/.config/eza/"
export XDG_CONFIG_HOME="$HOME/.config/"

# Load dotfiles:
for file in ~/.{ohmyzsh,zprofile,aliases,private}; do
    [ -r "$file" ] && [ -f "$file" ] && source "$file"
done
unset file

